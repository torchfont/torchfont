# AGENTS.md

This file applies to `docs/`.

- Documentation uses VitePress.
- Use the npm scripts in `package.json` for docs-specific work such as local preview or docs builds.
- Keep the English and Japanese documentation trees aligned when changing shared guides, references, navigation, or examples.
- Update `docs/.vitepress/config.mts` when page structure or navigation changes.
