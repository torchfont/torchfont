version: 2

build:
  os: ubuntu-lts-latest
  tools:
    nodejs: "22"
  jobs:
    install:
      - npm ci
    build:
      html:
        - npm run docs:build
        - mkdir -p $READTHEDOCS_OUTPUT/
        - mv docs/.vitepress/dist $READTHEDOCS_OUTPUT/html
